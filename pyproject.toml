[project]
name = "order-processing-assistant"
version = "0.1.0"
description = "AI-powered conversational assistant"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn>=0.51.0",
]
